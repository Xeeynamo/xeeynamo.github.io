# xee.dev

Personal portfolio website built with React and Vite.

- `npm install` - install dependencies
- `npm run dev` - start dev server
- `npm test` - run tests
- `npm run test:watch` - run tests in watch mode
- `npm run build` - produce build in dist/
